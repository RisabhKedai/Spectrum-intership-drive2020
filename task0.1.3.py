#accepting usre input
n=input("enter your number")
#though it is a number the input takes it as a string since it is not wrapped in int()
print(len(n))
#the len() gives the no. of characters hence the number of digits in the given number
